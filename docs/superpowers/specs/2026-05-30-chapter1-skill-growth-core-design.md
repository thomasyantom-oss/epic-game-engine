# Chapter 1 — 技能成长内核 · 设计纲领 + 分解(母文档)

**日期:** 2026-05-30
**性质:** Chapter 1 的母文档(meta-spec)。本文件**不**实现任何 feature,只干两件事:
1. 固化本轮 brainstorm 拍死的**架构决策**(后续每个 feature 翻这里,不重新推导)。
2. 钉死 Ch1 的 **feature 分解 + 依赖序**。每个 feature 各自走独立的 brainstorm → spec → plan → 实现周期。

母文档:`docs/superpowers/specs/2026-05-29-game-roadmap-design.md`(全局 7 章路线图)。
Chapter 1 在路线图中的定位:**技能成长内核(旗舰),build 空间的主轴,循环心脏,无法中立设计,必须第一。**

---

## 0. 一句话定位

Ch1 造的是**技能成长这台机器**——技能如何把属性炸成战斗效果、灵魂标签升级树(骨架)、专精开门、出战取舍。
**不造供料**:灵魂球的真实来源(特殊词缀灵魂怪掉落)是 Chapter 3。Ch1 用 debug 给球跑通闭环。

---

## 1. 架构决策(本轮拍死,后续 feature 遵守)

### 1.1 技能表示:骨架 + helper + spec(选定方案 a,否决 b 和 DSL)

技能不再是"每个一坨硬编码 JS"。三层分离:

- **skill spec = 数据(YAML)。** 描述技能*是什么*:目标方式、伤害公式参数、命中挂哪个 buff。纯配方,不含逻辑。
- **helper function = 共享执行代码。** 读 spec 把事*做了*:算伤害、扣血、上 buff、拼 log + 动画。跨技能复用。
- **大多数技能零 JS**:声明一个 `effect` archetype(`damage_only` / `buff_only` / `debuff_only` / `damage_with_debuff` / `heal` …)+ 参数,即可。helper 库覆盖 ~80% 技能。
- **结构性技能 fallback 专属 JS**:真正改流程的(弹射、改机制)写专属 handler。引擎有热重载 + JS handler,边际成本几乎为零,且所见即所得。

**示例:**
```yaml
fireball:
  effect: damage_with_debuff      # 命中哪个共享 helper
  damage: { base: 8, scaling: { 智力: 1.2 } }
  debuff: { id: burning, damage: 3, turns: 2 }
  targeting: single_enemy
```

**否决记录:**
- **否决 DSL**(技能效果彻底数据化的描述语言)—— heavy 且无实际意义。
- **否决方案 b**(往技能执行流程的"固定钩子点位"挂 hook 来改写流程)—— b 是为"涌现 / 作者不知道玩家会拼出什么"(Noita 式)准备的,代价是要防 hook 间 conflict、防退化组合、预先拍死钩子点位(= DSL 从后门溜回)。而品类决策(roadmap §4.4/§4.7)**已主动否决涌现**:没有任何技能是玩家自由拼出来的,升级树是作者**亲手画死的有限树**。既然如此,conflict 这个问题根本不该存在(每个节点最终形态作者写死)。用 b = 为一个已被设计掉的问题付架构税。
- **退路(三次法则):** helper 内部关键节点(`after_targeting` / `compute_damage` / `after_hit` …)可**顺手 fire 事件**(EventBus 现成),但**不**把它当正式变换机制设计。等真做了三五个结构性技能、看见反复想插手的点位浮现,b 可平滑长出来,届时点位设计有证据、不拍脑袋。

### 1.2 "技能也有一条 ModifierChain"

技能的**最终 spec = 多来源合并**,不止 node 一个:

```
最终 spec = base spec
          + 自己的升级 node 变换   (Feature 7)
          + 装备改写(roadmap §4.2)(Chapter 2)
          + 别的技能 / 被动打来的 patch(Feature 5)
          + 专精 patch(开关 enabled / 改 scaling / 改主属性)(Feature 6)
```

这是给"技能 spec"做的一条 ModifierChain(属性 ModifierChain 的同构版)。
- **数据层 patch**(改数值、改名、加 tag、开关可用性)走这条统一管道。
- **结构性联动**(狮驼岭鹰击那种"追击 = 插一次额外攻击")仍落专属 JS,不进管道(否则滑回 b)。

这条管道不是单独一段:Feature 1 立骨架(单来源),Feature 4/5/6/7 各自往上加一个 patch 源。

### 1.3 技能 ≠ 跨技能联动(正交,别混)

- **单技能怎么吸收自己的升级** = 方案 a(本文核心)。
- **跨技能联动 / synergy / 连携**(标记→引爆:这回合放标记,下回合火球对被标记目标翻倍)= **独立的一层**,靠 **buff/tag 跨回合传状态**实现(放标记 = 给目标挂 tag,火球 `compute_damage` 时读 tag)。引擎已有 buff/tag。a/b 选哪个都不影响它。

### 1.4 技能实例模型

技能从"全局定义"升级为"玩家拥有、有升级状态"。角色加组件:

```
Skillbook {
  slots: 6,                          # 出战技能位(职业可改;回合制逼出取舍)
  known: [                           # 玩家拥有的技能实例
    { base: "fireball", node: "pyroblast", equipped: true },
    { base: "ice_beam", node: null,        equipped: true }
  ]
}
```
- `base` = 哪个基础技能;`node` = 升到升级树哪个节点(null = 原始);`equipped` = 是否带上场。
- **一个 base 全局只有一个实例**(不能带两个不同升级方向的火球)。
- 执行时:取 `base` spec + `node` 变换 + 其他 patch 源 → 合并 → 喂 helper。

### 1.5 出战 / 被动

- **6 个出战格**,**职业可改**(有的职业天生多/少一格)。6 对回合制打循环够。
- **被动不占出战格**,走自己的轨(被动数量上限待定)。
- **被动两类:** 纯属性被动(+10 攻)→ 走属性 ModifierChain;**特殊效果被动**(鹰击式"有几率追击")→ 一段挂战斗事件的专属 JS。

### 1.6 升级 = 作者手画的有限树

- 升级结果是作者**显式定义的节点**,名字 / 图标挂在节点上(不是单个标签变换硬改名字,否则多变换打架):
  ```yaml
  fireball:
    upgrades:
      - id: pyroblast
        name: "炎爆"
        requires_tag: 爆破          # 消耗的灵魂球
        transforms: [aoe, bonus_hit]  # 引用可复用变换积木
  ```
- **变换(`aoe`/`bonus_hit`)= 底层可复用积木;节点 = 玩家看到的、有名字的东西。**
- **组合不爆炸**:不是 2^N 自由拼,是作者手画的有限树。
- **二次升级 = 节点挂子节点**(炎爆 + 多重 → 某某),深度由作者画,天然有界。**不单独设计。**
- **改 scaling 也是升级的一部分**(炎爆吃 1.5 倍智力)。

### 1.7 可逆性(非对称,刻意)

- **技能升级可花钱重置**(细调旋钮)。对应 roadmap §4.4 红线:"RNG 决定肝多久,不决定角色好不好"——灵魂球不焊死你,坏选择只是损失资源,不是永久变差的角色。重置是 money sink(实现落 Chapter 5),但**Ch1 数据模型要预留 node 可回退**。
- **专精不可洗**(身份级抉择;洗了抉择就没重量)。专精是确定性玩家选择、非 RNG,不可逆不违反 §4.4。

### 1.8 等级:保留,定位明确

- **等级 = 战力的尺子 + 前期地板。** 前期(装备差、技能不全)等级就是强度;后期装备属性加值 ≥ 或 ≫ 升级属性,主导权交给随机产出。
- **每级成长自动固定,不手动分配。** 因为要"等级 = 明确战力";手动分配会让同级两个号强度天差地别,尺子糊掉。build 差异化交给技能树 + 装备承载。
- 已上线的 Experience / 等级系统**留用不推翻**;已做的"手动分配属性点"那套**搁置不删**,留作未来某轨道可能复用。
- **穿装门槛用等级**(干净的标量尺子),**不用**"分配的属性点"(因人而异、难平衡)。

### 1.9 属性表(薄起步,可扩展)

- **基础属性 5 条:力量 / 敏捷 / 智力 / 体质 / 意志。** 地基。
- **二级属性从基础属性派生**(武器攻击伤害、暴击、命中…),先放一两条,后面随便长。
- **伤害类型 + 对应抗性**一层(物理 / 火 / 冰…对抗性),刷子优化空间的大头,要;武器伤害加成归这层。
- **主属性 = 职业 / 专精决定的那条基础属性**(战士→力量,法师→智力)。
- **全套白嫖 2026-05-26 的 ModifierChain**——它支持"衍生自衍生"(按 priority:先算基础属性,二级属性再读已算好的基础属性派生)。职业 / 专精 / 等级 / 装备全往这条链注册 modifier。**引擎不动,纯 mod 数据 + handler。**
- **技能吃属性的方式**:每个技能在 spec 里声明自己的 `scaling`(火球吃 120% 智力)。技能树和装备(Ch2)都挂在**同一张属性表**上 = build 空间的主轴。

### 1.10 专精 = 门 / 灵魂球 = 梯子(正交划分;回答 roadmap §6 deferred"转职/职业树如何与灵魂标签升级共存")

- **专精是整个升级子系统的总闸。** 前期(未专精)= 基础技能组,**不能升级**;**选专精 = 灵魂球升级树整个解锁。** 用一个明确里程碑把 Ch1 最复杂的块 gating 住:前期清爽不劝退,深度在玩家做出"我是谁"的决定后才展开。
- **专精 = 门:** 决定能碰哪些技能 / 哪些升级分支开放;改主属性(战士→盾战士:力量→体质);可能给专属被动。确定性、玩家主动选。
- **灵魂球 = 梯子:** 不管哪个专精,真正往树上爬(火球→炎爆)的**材料**永远是灵魂球,RNG 只在这层。
- **专精开哪些树,灵魂球决定爬不爬得上去。** 不重叠、不循环(避免 §4.6 依赖图成环)。
- 架构上专精就是技能 spec ModifierChain 上的又一个 patch 源,不需要第三套机制。
- **不给专精独立天赋树**(否则又一条成长轨道 = 又一摊平衡)。

---

## 2. Feature 分解 + 依赖序

Ch1 不是"一个 spec 分三段",是**一组各自独立的 feature**,每个走独立 brainstorm → spec → plan → 实现周期。分细的目的:每段可单独验证、单独讨论数值 / UI、控制每轮 context。

| # | Feature | 含 | 依赖 | 单独深聊点 |
|---|---------|----|------|-----------|
| 1 | **技能架构重构** | 硬编码技能 → `骨架 + helper + spec` 解析;**行为不变(纯重构)** | — | 测试设计、多 case 回归、谨慎不破坏现有实现 |
| 2 | **属性表扩展** | 5 基础属性 + 二级属性派生 + 各职业主属性 + 每条属性的收益 | 1 | 每属性收益 = 数值方案,需具体讨论 |
| 3 | **伤害类型 / 抗性** | 伤害类型层 + 抗性 + 武器伤害加成 | 1, 2 | 数值 / 可玩性设计,需具体讨论 |
| 4 | **Skillbook(出战配置)** | 技能实例模型(base/node/equipped)+ 6 出战格 + 前端配置面板 | 1 | 重要前端 feature,需 UI 设计 |
| 5 | **被动技能** | 被动轨 + 纯属性被动(走 ModifierChain)+ 特殊效果被动(挂战斗事件专属 JS) | 1, 4 | 前后端双改 + case 多,独立讨论 |
| 6 | **专精** | 选专精 + 改主属性 + 开门(gating 升级树)+ 不可洗 | 2, 4, 等级 | 联动职业 / 等级 / 技能,影响最大 |
| 7 | **技能升级树(旗舰)** | 升级树数据格式 + 灵魂球材料 + 节点变换 patch spec + 花钱重置 + debug 给球 | 1–6 | 本体,可能再拆;skilltree(加点)≠ skillbook(列表) |

**排序说明:**
- **#1 是所有人的地基,必须最先。** 纯重构、行为不变,可回归测试保安全。
- **#2/#3 是数值轴**,接着做。
- **#4/#5 与 #2/#3 无强依赖**,先做哪条都行(#4/#5 进入"拥有 / 养成")。
- **#7 是旗舰本体**,依赖前面全部,可能在它自己那轮再拆(树数据 / 灵魂球 / 重置 / UI)。

**关键区分:Skillbook ≠ Skilltree。**
- Skillbook(#4)= "我带哪几个技能"(列表 / loadout)。
- Skilltree(#7)= "火球点成炎爆"(加点 / 升级)。
- 两个不同的前端 + 数据形状,分开。

---

## 3. Ch1 范围边界

- **IN:** 升级这台机器的全部机制 + 数据模型 + 前端 + 调试供料。
- **OUT(延后):**
  - **灵魂球真实供给**(特殊词缀灵魂怪掉落)→ Chapter 3。Ch1 用 debug 给球(起始塞一把 / 控制台发球)跑通闭环、调手感。
  - **装备随机词缀 + "改写技能"高级词缀** → Chapter 2(Ch1 的技能 spec ModifierChain 预留装备 patch 源)。
  - **重置的 money sink 经济** → Chapter 5(Ch1 只预留 node 可回退的数据模型)。
  - **被动数量上限**待定。
  - **手动分配属性点**搁置(已实现,不删不用)。

---

## 4. 下一步

1. 本母文档 review 通过后,**本轮 brainstorm 收尾**。
2. 下一轮(建议新开会话省 context)单独 brainstorm **Feature #1 技能架构重构** → spec → plan → 实现。
3. 此后逐个 feature 推进,每个翻本母文档的 §1 架构决策。

---

## 附录 A:原始对话实录(决策溯源)

> 本轮 brainstorm 的原始对话,逐字保留(仅省略最初的代码探查阶段)。供后续 feature 追溯"为什么这么定"。**用户 = U,Claude = C。**

---

**U:** 我现在想进行 chapter1 的开发。

---

**C(探查现状后):** 我已经把路线图、属性/装备系统设计、以及现有技能的实现底子都过了一遍。先把我对现状的判断摆出来,再开始逐个问题深挖。

现状关键事实:
- 现有技能是**完全硬编码**的——`fireball.js` 里直接 `damage = attack + 10`、写死灼烧 buff、手搓 log/动画。每个技能一套 bespoke JS+YAML,彼此无共享结构。
- 已有 `ModifierChain` 属性管道、装备 modifier、等级成长。但**技能本身没有"实例/拥有/状态"概念**——技能是全局定义,谁都能放,没有"玩家的火球升到了哪一级、带了什么标签"这种东西。

而 Chapter 1 旗舰要的恰恰是:技能变成**玩家拥有的、有升级状态的东西**(火球 +`爆破`球 → 炎爆,确定性升级),灵魂标签当材料,RNG 只在获取。

所以在我看来,Ch1 的**地基**不是"再写几个技能",而是**重构技能的表示方式**:从"硬编码 handler"变成"可被升级变换作用的数据/实例模型"。这一步不做,后面的升级树就没地方挂。

这里有个真正的设计岔路,想先听你的取向 ——

火球 → 炎爆这个"确定性升级",你心里想的是哪种实现哲学?

**A. 炎爆是一个独立技能定义**,升级 = 把玩家技能槽里的 `fireball` 替换成 `fireball_pyroblast`。简单直接,但每个升级结果都得手写一份完整技能(N 个标签组合 → 组合爆炸,且共享逻辑全是 copy)。

**B. 技能是骨架 + 一组可叠加的"标签变换"**,炎爆 = `fireball` 实例上挂了 `爆破` 变换(改 targeting 为 AOE、加命中、改伤害公式)。技能定义本身保持一份,升级是往实例上叠 modifier。更接近你 spec 里"技能=纯骨架、装备 modifier 改写技能"的语言,但要先定义"技能行为可被哪些维度改写"的协议。

你倾向哪条?还是你脑子里本来就是别的形状?

---

**U:** 第二种能改变技能名字么?还有你说的重构要怎么做?

---

**C:** 能,而且这正好牵出 B 该怎么落地的核心。我分两部分答。

## 改名字 —— 能,但要想清楚"谁来决定名字"

技能名不该由单个标签变换硬改(否则两个变换都想改名字就打架)。更干净的做法:**升级结果是作者显式定义的"升级节点"**,名字挂在节点上。

也就是说,`火球 + 爆破 → 炎爆` 不是"系统自动把爆破变换叠上去顺便改个名",而是作者在升级树里**亲手写下一个节点**:

```yaml
fireball:
  upgrades:
    - id: pyroblast
      name: "炎爆"           # 名字、图标都挂这儿
      requires_tag: 爆破      # 消耗的灵魂球
      transforms: [aoe, bonus_hit, ...]   # 这个节点应用哪些可复用变换
```

这跟你 spec §4.7 自己写的取舍是一致的——"作者知道所有技能,更接近 ARPG 技能树 + 材料 gating"。好处:**组合不爆炸**。不是 2^N 个标签自由拼,而是作者手画的一棵有限树,每个节点名字/平衡都可控。变换(`aoe`/`bonus_hit`)是底层可复用积木,**节点**才是玩家看到的、有名字的东西。

这也顺手回答了 spec 里留的那个旋钮(组合标签能否再升级):炎爆这个节点底下可以**再挂子节点**(炎爆 + 多重 → 某某),深度由作者决定画多深,天然有界。

## 重构怎么做

现在 `fireball.js` 是一坨命令式流程:`算目标 → 算伤害 → 扣血 → 上 buff → 拼 log → 拼动画`,全写死。变换没地方插手。

重构的本质是**把这坨流程切成有名字的"接缝"**,让变换能精准改某一段而不是重写整段。火球那段流程其实是这几个接缝:

1. **targeting** —— 选谁(单体 / AOE / 弹射)
2. **damage** —— 伤害公式(`attack + 10`、能否暴击、伤害类型)
3. **on_hit effects** —— 命中后挂什么(灼烧 buff、附加效果)
4. **presentation** —— log + 动画(这块基本能自动从 spec 生成,不用手写)

`爆破` 变换 = `{targeting: 单体→AOE, damage: +命中}`。它只 patch 接缝 1 和 2,不碰别的。

这里有个**关于重构走多深的真岔路**,我想听你定:

- **轻量版**:保留"每个基础技能一个 JS handler",但 handler 不再硬编码,而是读一份**已合并好的 skill spec**(基础 + 节点变换 data merge 出来的结果)。引擎负责"基础 + 节点 → 最终 spec"的合并和 presentation 自动生成;handler 只读 spec 干活。改动可控,JS 还在,但变成"读配置"而非"写死"。

- **声明式版**:技能行为彻底数据化——targeting/damage/effects 都是 YAML 描述的结构,变换就是对这个结构打 patch,**几乎不写 JS**(只有真正怪的技能才 fallback 到 JS)。最贴"纯骨架"理想,扩展性最好,但要先把"技能效果的描述语言"这套 DSL 设计出来,工作量和风险都大些,而且容易过度设计。

你做过上百款游戏,应该对"配置驱动 vs 写死"这条线很熟——你想让 Ch1 一步到位上声明式,还是先轻量版把升级树跑通、等第二个真实需求再提炼 DSL(三次法则)?

---

**U:** 别,我没开发过上百款游戏,我是玩过上百款游戏,本身是个 backend engineer。dsl 有点太 heavy 了,而且没有什么实际意义,第一种就足够了,对于常见的效果比如纯伤害,纯 buff、debuff,伤害+buffdebuff 这种完全可以写 helper function 对吧。所以你说的 skill spec 就是 helper function?

---

**C:** 哈,记错了,抱歉——我把"玩过"听成"做过"了。backend engineer + 上百款游戏阅历,这个组合其实更对路:你对"什么系统耐刷"有玩家直觉,对"配置 vs 写死"有工程判断。(我回头把那条记错的 memory 改了。)

选第一种,同意,DSL 确实是没事找事。

但你最后那句把两个东西黏一起了,我拆一下,因为这个拆分恰好就是"轻量版"的全部精华:

- **skill spec = 数据**。描述这个技能*是什么*:目标方式、伤害公式参数、命中挂哪个 buff。纯 YAML,不含逻辑。
- **helper function = 执行代码**。读 spec 把事*做了*:算伤害、扣血、上 buff、拼 log+动画。可复用,跨技能共享。

它俩是配套的两层,不是一个东西。spec 是"配方",helper 是"按配方做菜的厨子"。

而你那句"常见效果写 helper"其实比我原话更聪明,顺手干掉了"每个基础技能一个 JS handler"这个负担。如果一个技能就是"纯伤害"或"伤害+debuff",它**完全由 spec 描述 + 一个共享 helper 执行**,根本不用为它写专属 JS。于是技能分两类:

```yaml
fireball:
  effect: damage_with_debuff      # 命中哪个共享 helper
  damage: { base: attack, add: 10 }
  debuff: { id: burning, damage: 3, turns: 2 }
  targeting: single_enemy
```

```
helper 库(共享):
  damage_only          纯伤害
  buff_only            纯增益
  debuff_only          纯减益
  damage_with_debuff   伤害+减益    ← 火球、毒镖走这条
  heal                 治疗
  ... 覆盖 80% 技能
```

只有真正怪的技能(比如某个机制独一无二的)才 fallback 写专属 JS。**变换(升级节点)就是在执行前 patch 这份 spec**:`爆破` 节点把 `targeting: single_enemy → aoe`、`damage.add: 10 → 15`。helper 读到的是 patch 后的 spec,自然就炸成 AOE 了。

这样三层很干净:**spec(配方) ← 变换 patch(升级) → helper(厨子)**。

现在问题来了,也是这套能不能成立的关键一问:你觉得这套 archetype + 参数,能覆盖到多少?我担心的是**有些"改写"是结构性的、patch 参数表达不了的**——比如"火球额外弹射到第二个目标""灼烧可以暴击""每次命中回 5 蓝"。这些不是改个数值,是改流程。

你的预期是:这类结构性改写(a)很少,允许它们 fallback 成专属 JS / 专属 helper 就行;还是(b)你希望连这些都能通过"往流程接缝上挂 hook"在数据层表达?这个取向决定了 helper 要设计得多有弹性。

---

**U:** 解释一下 b

---

**C:** (b) 的核心想法是:helper 执行技能时,不是一条闷头跑到底的流水线,而是在几个**固定的点位**停下来"喊一嗓子"(fire 一个事件),谁想插手就在那个点位挂个 hook。变换不再是"改参数",而是"往某个点位注册一段行为"。

拿火球的流程举例,helper 内部会经过这些点位:

```
damage_with_debuff helper 执行:
  ① before_targeting   ← 选目标前
  ② after_targeting    ← 已知打谁了      ← "弹射"在这里:往目标列表里再塞一个
  ③ compute_damage     ← 算伤害          ← "灼烧可暴击"在这里:改伤害判定
  ④ after_hit (每目标) ← 每次命中后       ← "命中回5蓝"在这里:给施法者加蓝
  ⑤ build_presentation ← 拼log+动画
```

于是 `弹射` 这种结构性改写,在 (b) 里长这样——它不 patch 参数,它**在 `after_targeting` 点位挂一段逻辑**:

```yaml
fireball:
  upgrades:
    - id: fireball_chain
      name: "连珠火球"
      requires_tag: 多重
      hooks:
        after_targeting: add_extra_target   # 引用一段注册好的行为
```

`add_extra_target` 是一小段共享代码("在目标列表里追加一个最近的敌人")。`灼烧可暴击` 就挂 `compute_damage`,`命中回蓝` 就挂 `after_hit`。

**这跟你已有的引擎其实是同构的**——你的 EventBus 本来就是"fire/on/priority"。(b) 等于把"技能内部流程"也变成一条 mini 事件总线,让变换用注册 handler 的方式改写流程,而不只是改数据。

---

说实话,我对 (b) 是**审慎反对**的,讲清楚让你判断:

**(b) 的代价:** 你得预先定义好"有哪些点位"。点位定少了,某个变换想改的地方没有钩子,照样卡住;点位定多了,等于把整个执行流程的每一行都暴露成 API——这就是 DSL 从后门溜回来了,只是换了张"事件"的皮。而且 hook 之间的顺序、互相覆盖(两个变换都想改 `compute_damage` 谁先)又是一摊新复杂度。

**(a) 的代价:** 极少数结构性技能要写专属 JS / 专属 helper。但你引擎本来就有热重载 + JS handler,多写一个 `fireball_chain.js` 的边际成本几乎是零,而且那段代码**所见即所得**,不用绕着钩子点位推理。

我的判断:**先 (a)**。理由是三次法则——你现在连一个结构性改写都还没真写过,凭空设计"通用钩子点位"必然拍脑袋。等真做了三五个结构性技能,你会**看见**它们反复想插手的就是那两三个点位,那时候再把 (b) 提炼出来,点位的设计是有证据的、贴合真实需求的。现在上 (b) = 没有实例的抽象 = 大概率设计错。

所以我建议 Ch1 落"(a) + 留好退路":helper 内部该 fire 的关键节点**顺手 fire 一下**(反正你 EventBus 是现成的),但**不把它当成正式的变换机制去设计**。结构性技能现在一律走专属 JS。等点位需求自己浮现,(b) 是平滑长出来的,不是推倒重来。

你认同先 (a) 吗?还是你对某个具体的结构性改写特别在意,想现在就压力测试一下 (a) 扛不扛得住?

---

**U:** 所以 a 没法处理复杂的技能联动?还是什么意思?就是说 a 的话技能只有处理完自己的逻辑才会处理下一个技能逻辑,b 的话是同时处理但是很有可能会 conflict?

---

**C:** 不是,你这个理解里有个偏差,我得纠正一下,因为它会影响后面所有判断。

**(a) 和 (b) 的区别完全不在"多个技能怎么调度"。** 那一层两者一模一样:回合制,玩家这回合放一个技能,helper 把这一个技能从头跑到尾(算目标→伤害→扣血→buff→动画),完整结束,才轮到下一个单位行动。没有并发,谁都不会"同时处理"。这点 (a)(b) 没差别。

区别在**一个技能自己内部、怎么吸收它身上的升级**:

- **(a)**:升级要么改数据参数(炎爆 = 火球 spec 改几个数),要么——如果是结构性的(弹射)——你**直接写一份这个升级版的专属 JS**。"基础 + 升级"在*作者写代码时*就合并成了最终那一份逻辑。运行时跑的就是一坨写死的、确定的流程。

- **(b)**:升级不写死,而是往技能执行流程的**固定点位挂 hook**,引擎在*运行时*把"基础流程 + 所有挂上来的 hook"现场拼起来跑。

所以你说的 "conflict" 是真问题,但它的尺度不是"技能 A vs 技能 B",而是**同一个技能身上挂了多个升级、它们都想改同一个点位**。比如火球同时升了"灼烧可暴击"和"伤害减半但必暴"——两个 hook 都想改 `compute_damage`,谁先跑、谁覆盖谁?(b) 把这个歧义引进来了,(a) 没有——因为 (a) 里这俩组合的最终结果是作者**亲手写死的那一份**,作者写的时候就把顺序定死了,不存在运行时打架。

---

顺带,我怀疑你脑子里其实在想的是**第三个东西:技能之间的"联动 / synergy"**——你 spec 里反复强调的"回合制对技能 synergy / 连携 / 排序友好"。那个是比如:这回合放"标记",下回合的火球对被标记目标翻倍。

**这层跟 (a)/(b) 完全无关,(a) 和 (b) 都不解决它。** 跨技能联动是靠**回合之间留下的状态**实现的——放标记 = 给目标挂个 buff/tag,火球执行到 `compute_damage` 时读"目标身上有没有标记 tag"。这是 buff/tag 系统跨回合传递信息,你引擎已经有了。它跟"单个技能怎么吸收自己的升级"是正交的两条轴。

所以澄清一下你到底关心哪条:

1. **单技能怎么吸收升级**(= 我们在选的 a/b)——我建议 a。
2. **跨技能联动 / 连携**(= 标记+引爆这种)——这是独立一块,靠 buff/tag 跨回合传状态,a/b 选哪个都不影响它。

你刚才的疑问,是担心 (a) 表达不了第 2 类联动吗?如果是,我可以具体演一遍 (a) 架构下"标记→引爆"怎么跑通,你看扛不扛得住。

---

**U:** 我知道了,就 a 就行,b 适用于我都不知道他会怎么升级的情况,a 适用于我已经定义好了升级的树。是这个意思吧。

---

**C:** 那正是它——而且你已经把它说得比我更精确了。

(b) 是为"涌现 / 我不知道玩家会拼出什么"准备的(Noita 那种自由组合),代价是得防 conflict、防退化组合。而你 spec §4.4/§4.7 已经**主动否决了涌现**——"没有任何技能是玩家自由拼出来的,全是追逐已知目标""作者知道所有技能"。既然升级树是你**亲手画死的有限树**,那 conflict 这个问题根本不该存在——每个节点的最终形态是你写的时候就拍死的。用 (b) 等于为一个你已经设计掉的问题付架构税。

所以 **(a) 和你的品类决策是自洽的**,不是妥协。一句话锁定:

> 升级 = 作者手画的有限树。常见效果(纯伤害 / 纯 buff / debuff / 伤害+debuff / 治疗)走**共享 helper + YAML 参数**,零 JS;结构性的特殊技能(弹射、改机制)**fallback 写专属 JS**。helper 内部关键节点顺手 fire 事件留退路,但不当正式机制设计。

地基方向定了。接着往下,下一个要拍的是**"技能怎么变成玩家拥有的、有升级状态的东西"**——也就是数据模型。现在引擎里技能是全局定义、没有"实例/拥有"概念。我先抛我的想法你拍:

给角色加个组件,大概长这样:

```
Skillbook {
  slots: 4,                          # 出战技能位(回合制要逼出取舍,不能全带)
  known: [                           # 玩家拥有的技能实例
    { base: "fireball", node: "pyroblast", equipped: true },
    { base: "ice_beam", node: null,        equipped: true }
  ]
}
```

- `base` = 哪个基础技能(火球)
- `node` = 当前升到了升级树的哪个节点(炎爆;null = 还是原始火球)
- `equipped` = 是否带上场

升级行为 = 消耗灵魂球 → 把某个实例的 `node` 从 null 改成 `pyroblast`(或从一个节点推进到子节点)。执行技能时,引擎拿 `base` 的 spec + `node` 的变换 patch,合并出最终 spec 喂给 helper。

这里我故意先放了几个**有待你定的旋钮**,挑你最有想法的先聊:

1. **出战技能位有没有上限?** 我倾向有(回合制的深度来自"这局带哪几个技能"的取舍,无限带 = 没取舍)。但这会牵扯到"学了一堆技能但只能带 N 个"。
2. **升级是不是单向不可逆?** 火球升成炎爆后,还能不能变回火球、或改走另一条分支?这影响灵魂球是不是"消耗即焊死"。
3. **一个 base 能不能同时存在多个实例?**(带两个不同升级方向的火球)还是一个 base 全局只有一份?

你先说说出战位上限和"可逆性"这两个的取向,第三个可以顺带。

---

**U:** hmm,第一条取决于职业吧。我觉得上限 6 个?毕竟回合制,打循环的话 6 个也够了。升级 maybe 可以花钱重置?base 肯定就一个实例。然后有几个想法 1.可能有技能能强化其他技能。改变其他技能效果/技能名。或者激活其他技能可用性,举例来说就是梦幻西游的狮驼岭的鹰击。2.我本来想技能加入熟练度,但感觉有点臃肿。等级也有点臃肿,像魔兽世界都摒弃了等级。3.技能 maybe 有二次升级?4.被动技能也要有,而且被动可能是有特殊效果的。

---

**C:** (收下 6 出战位/职业可改、花钱重置、base 单实例;)
- 想法 3(二次升级)= 升级树挂子节点,已白送,不单独设计。
- 想法 4(被动)= 不占回合的常驻技能;纯属性被动走 ModifierChain,特殊效果被动(鹰击式追击)写挂战斗事件的专属 JS。
- 想法 1(技能强化其他技能)= 最有架构含金量:技能最终 spec 的合并来源不止 node,还有装备改写、别的技能/被动的 patch = **给"技能 spec"也做一条 ModifierChain**(属性 ModifierChain 同构版)。数据层 patch 走统一管道;结构性联动仍落专属 JS(否则滑回 b)。纳入 Ch1。
- 想法 2(熟练度/等级臃肿)= 单独拎出来:纠正 WoW 砍的是**武器熟练度**不是等级;熟练度 = 升级树劣化版,砍。但"等级臃肿"捅到真问题——你已上线 Experience/等级/属性点系统,Ch1 要重新拍"基础战力从哪来"。轨道候选:等级→属性点(已做)、技能树(必进)、装备词缀(Ch2 必进)、熟练度(砍)。问:**等级留不留?** 留 = 确定性数值地板 + 内容节奏闸;砍 = 成长 100% 来自技能+换装,但推翻已上线系统。

---

**U:** 1.被动不占格子。2.那必须保留等级,等级是非常关键的,就是比较明确的战力衡量标准,前期装备差技能不全的时候等级就是基础属性,就是强度的尺子。等级带来的属性点更多的是度过前期,后期装备带来的属性加值应该略大于或者远大于升级的属性。可以把升级的属性作为穿装备的门槛,虽然我不太喜欢这种设定。

---

**C:** (锁:被动不占出战格;等级保留 = 尺子+前期地板,后期装备主导;)顺手解掉你"不喜欢但提了"的那条——**别用分配的属性点当穿装门槛,直接用等级当门槛**(等级是干净标量尺子,属性点因人而异难平衡)。由"等级=尺子"直接推出:**每级成长自动固定,不手动分配**(否则同级两个号强度天差地别,尺子糊掉);build 差异化交给技能树+装备承载。已做的"手动分配"那套搁置不删。

接着拍 Ch1 核心(§4.1"技能如何把属性炸成战斗效果"):现在火球只认 `attack`,一维标量撑不起刷子优化空间。需先定**属性表**和**技能吃属性的方式**:主属性(力/敏/智/体)+ 每技能在 spec 里声明 `scaling`(火球吃 120% 智力)+ 升级可改 scaling + 装备往属性轴 roll。问:1.属性表多厚(暗黑式厚 vs 薄起步)?2.要不要伤害类型/抗性这一层?

---

**U:** 伤害类型、对应抗性肯定是要的。甚至可能有武器伤害加成。但这个具体填值可以之后再说。基调应该是人物基础属性,后面就随便添了对吧。人物基础属性,力量,敏捷,智力,体质,意志。人物有个主属性,取决于职业和专精,专精可能就是到几级以后可以选专精,比如战士可以专精盾战士这种。主属性可能就从力量变成体质。然后基础属性决定一些二级属性比如武器攻击伤害(普通攻击)之类的。

---

**C:** (锁属性表结构:5 基础属性 + 二级派生 + 伤害类型/抗性 + 主属性=职业/专精决定,全套白嫖 2026-05-26 的 ModifierChain。)单独拍**专精**——这正是 roadmap §6 挂起的 deferred"转职/职业树如何与灵魂标签升级共存"。草案:**专精=门(决定开哪些技能/升级分支、改主属性、给专属被动,确定性玩家选)/ 灵魂球=梯子(爬树的材料,RNG 只在此层)**,正交、不循环。问:1.这个正交划分合意吗(劝你别给专精独立天赋树=又一条轨道)?2.专精可不可以洗?

---

**U:** 1.专精解锁之后才能用灵魂球升级。专精解锁之前用基础技能组。2.专精不能洗。重要抉择点洗了太麻烦。

---

**C:** (锁:专精 = 升级子系统总闸——未专精只能用基础技能组、不能升级,选专精=升级树整个解锁;专精不可洗,可逆性非对称——技能升级可花钱重置、专精不可逆。)Ch1 设计骨架成型,敲两个边界:**边界 1 灵魂球供给不在 Ch1**(真实来源=Ch3 灵魂怪掉落,Ch1 用 debug 给球跑通闭环)。**边界 2 Ch1 太大,分阶段交付**——初版提议切 A/B/C 三段。

---

**U:** 你可以再切细一点。一个 feature 一段。比如 ch1-A 重构是一段,扩展属性是一段,伤害类型计算是一段。因为这里面重构的工作可能需要测试设计,多个 case 来测试保证没 bug。而且可能会影响现在的已有实现,需要谨慎。属性扩展包括了每个职业的主属性,和每个属性背后带来的收益,需要具体讨论确定方案。然后伤害类型/抗性也需要具体讨论,数值设计来确定其可玩性。ch1-b skillbook 是重要前端 feature,需要 ui 设计,这个最好也单独成段,然后被动需要前端+后端双改动,而且涉及到的 case 可能很多,独立讨论。专精那就更别说了,联动了职业等级,技能,影响老大了。ch1-c,这里面几乎每一个都是一个 feature。skillbook 和 skilltree 还是两种东西,一个是加点一个是技能列表。感觉不能放一起。后面这几个工作量也不小。分开主要是为了给我节省 token。

---

**C:** (认同,Ch1 = 一组各自独立 feature,每个走独立 brainstorm→spec→plan 周期。按"一个 feature 一段"重切成 §2 的 7 段依赖序表,定下"先写本母文档固化决策 + 钉死分解,本轮收尾"的走法。)

---

**U:** ok 写的详细一点。像一些关键对话可以直接写到 appendix 里以作参考。

---

**U:** 你这对话引入的不够详细,直接给原对话放进来。

—— (本附录即据此补全为原始对话实录)
