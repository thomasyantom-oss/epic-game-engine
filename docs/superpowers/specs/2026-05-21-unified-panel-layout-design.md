# 统一面板布局设计

## 概述

左上主面板在探索和战斗两种状态间切换内容，不再全屏替换。战斗操作集中在主面板内完成。

## 全局布局（不变）

```
┌────────────────────────┬──────────┐
│      左上（主面板）      │  右上    │
│                        │ 人物/设置 │
├────────────────────────┼──────────┤
│      左下（事件栏）      │  右下    │
│                        │ 快捷行动  │
└────────────────────────┴──────────┘
```

四个面板位置不变，内部内容根据状态切换。

## 左上主面板 — 探索状态

```
┌──────────────────┬─────────┐
│                  │ 地域信息  │
│     地图网格      │ 怪物列表  │
│  (自适应格子)     │ POI按钮  │
│                  │          │
└──────────────────┴─────────┘
```

- 左侧：地图网格，格子自适应面板高度
- 右侧信息栏：
  - 地域层级（如：齐国 > 嘉庆县 > 青羊镇）
  - 周围怪物列表
  - POI 交互按钮（进入村庄等）

## 左上主面板 — 战斗状态

```
┌─────┬───────────┬─────┐
│ ①绿 │ [地形格子] │ ①红 │
│ HP  │  ▶  ▶  ◀ │ HP  │
│ MP  │  ▶     ◀ │ MP  │
│ ②绿 │     ◀    │ ②红 │
│ HP  │           │ HP  │
│ MP  │           │ MP  │
├─────┴───────────┴─────┤
│      [技能栏]          │
│ 攻击 | 防御 | 技能 | 道具 │
└───────────────────────┘
```

### 战场区域（中间）
- 3×3 格子，取当前地图位置周围的地形颜色渲染
- 我方标记：绿色三角 ▶（朝右），带序号
- 敌方标记：红色三角 ◀（朝左），带序号
- 序号与左右状态栏对应

### 状态栏（左右）
- 左侧：我方单位列表（绿色系）
- 右侧：敌方单位列表（红色系）
- 每个单位显示：序号 + 名称 + HP 条 + MP 条
- 上下居中排列，每侧最多 9 个单位（3×3 满员时）

### 指令栏（下方，逐角色顺序操作）

按我方角色速度顺序，逐个选择行动：

**流程：**
1. 显示"[角色名] 的回合" + 指令列表（攻击 / 防御 / 技能 / 道具 / 逃跑）
2. 点击指令后：
   - 攻击 → 指令列表替换为目标列表（可攻击的敌方单位）
   - 技能 → 替换为技能列表 → 点击技能后替换为目标列表
   - 道具 → 替换为道具列表 → 点击道具后替换为目标列表
   - 防御 → 无需选目标，直接确认，推进到下一角色
   - 逃跑 → 无需选目标，直接确认，推进到下一角色
3. 点击目标 → 该角色行动确认，自动推进到下一个角色
4. 最后一个角色选完 → 直接进入回合结算

**取消操作：**
- 右下角快捷栏放"取消"按钮
- 点击可回退到上一步（目标→指令，或回到上一个角色）

**无确认按钮** — 选完目标即确认，减少点击次数。

### 站位系统
- 3×3 格子：3 列（前/中/后）× 3 行
- 我方前排靠右（靠近敌方），后排靠左
- 敌方前排靠左（靠近我方），后排靠右
- 角色站位在人物面板中预设（行+列偏好）

### 地形格子渲染
- 取当前玩家位置为中心的 3×3 区域地形
- 每格背景色用对应地形颜色（不显示地形汉字）
- 纯视觉效果，不影响战斗机制

## 左下事件栏

- 探索时：移动描述 + 危险度颜色
- 战斗时：战斗日志（谁攻击了谁、伤害数值等）

## 右下快捷行动栏

- 探索时：快捷按钮（骑乘、使用道具等）
- 战斗时：可放道具快捷使用，或留空

## 右上面板

- 不变：人物信息 / 设置
- 人物面板新增：战斗站位预设（选择前/中/后排 + 行位置）

## 删除的 UI

- ~~方向键 D-pad~~（键盘 WASD/箭头移动足够）
- ~~地形图例~~（地图上颜色+汉字已清楚）
- ~~操作面板的 POI 按钮~~（移到主面板信息栏）

## 本次实现范围

1. 删除方向键 D-pad 和地形图例
2. 主面板信息栏改为：地域信息 + 怪物列表 + POI 按钮
3. 地图格子自适应大小
4. 战斗视图重构：3×3 地形格子 + 三角标记 + 左右状态栏 + 下方技能栏
5. 战斗视图嵌入主面板（不再全屏替换）
6. 右下角改为快捷行动栏（暂时可空）

## 暂不实现

- 地域层级数据（齐国/嘉庆县等需要区域系统支持）
- 怪物列表数据（需要遭遇表系统）
- 技能系统（攻击/防御先保留，技能按钮占位）
- 站位预设 UI（人物面板扩展）
- 快捷行动具体内容（骑乘等）
