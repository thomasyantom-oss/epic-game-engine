server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:file:./data/epic
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

epic:
  mods-path: ../mods
